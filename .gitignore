# Media files
*.mp4
*.mov
*.webm
*.mp3
*.wav
*.aac
*.m4a

# Images (keep READMEs, exclude actual media)
*.png
*.jpg
*.jpeg
*.gif

# Project outputs
projects/

# Reference frames (copyrighted)
brand/reference-edits/frames/
brand/reference-edits/*.mp4

# Logo source files
brand/logos/*.png
brand/logos/*.svg

# Environment
.env
.env.local

# OS
.DS_Store
Thumbs.db

# Node
node_modules/

# Temp files
/tmp/
*.tmp

# ── Client / brand material — never in this PUBLIC repo ──────────────
# This repo ships the engine plus the neutral "Acme Video Co" example brand.
# Your own brand pack and per-video client work stay local.
output/
brand/ctm-brand.md
brand/ctm-brand.local.md
brand/ctm.local/
# Per-client brand packs (colors, logos, GHL location config) — local only
brand/clients/
!brand/clients/README.md
brand/logos/*
!brand/logos/README.md

# Hook blocklist of internal identifiers — must never be tracked in a public repo
.githooks/private-patterns.txt
